version: '3'

includes:
  hugo: ./Taskfile_{{OS}}.yml

tasks:
  default:
    desc: 'Run hugo:serve on {{OS}}'
    cmds:
      - task: 'hugo:serve'

  minify:
    desc: 'Minify built site'
    cmds:
      - minify -s -r -o minified/ public/
      - rm -Rf public
      - mv minified public